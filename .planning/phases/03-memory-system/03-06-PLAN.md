---
phase: 03-memory-system
plan: 06
type: execute
wave: 5
depends_on: ["03-04", "03-05"]
files_modified:
  - src/screens/ChatScreen.tsx
  - src/App.tsx
autonomous: false

must_haves:
  truths:
    - "Memory store loads from storage on app initialization"
    - "Conversation end hook is active during chat"
    - "Memory system is integrated into chat flow"
  artifacts:
    - path: "src/screens/ChatScreen.tsx"
      provides: "ChatScreen with useConversationEnd hook"
      contains: "useConversationEnd()"
    - path: "src/App.tsx"
      provides: "App initialization with memory store"
      contains: "useMemoryStore"
  key_links:
    - from: "src/screens/ChatScreen.tsx"
      to: "src/hooks/useConversationEnd.ts"
      via: "hook call"
      pattern: "useConversationEnd\\(\\)"
    - from: "src/App.tsx"
      to: "src/stores/memoryStore.ts"
      via: "initialization"
      pattern: "loadFromStorage"
---

<objective>
Integrate the memory system into the app by adding initialization and the conversation end hook to the chat flow.

Purpose: Final integration step that connects all memory components to the running app.
Output: Updated ChatScreen and App with memory system integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-memory-system/03-RESEARCH.md

Reference prior plan summaries:
@.planning/phases/03-memory-system/03-04-SUMMARY.md
@.planning/phases/03-memory-system/03-05-SUMMARY.md

Existing files to modify:
@src/screens/ChatScreen.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useConversationEnd hook to ChatScreen</name>
  <files>src/screens/ChatScreen.tsx</files>
  <action>
Integrate conversation end detection into ChatScreen:

1. Import the hook:
   ```typescript
   import { useConversationEnd } from '../hooks/useConversationEnd';
   ```

2. Add hook call at the top of the ChatScreen component (after other hooks):
   ```typescript
   export function ChatScreen() {
     // ... existing hooks ...

     // Memory extraction on conversation end
     useConversationEnd();

     // ... rest of component ...
   }
   ```

The hook:
- Listens for app backgrounding
- Triggers memory extraction automatically
- Is non-blocking (doesn't affect UI)

This is the only change needed in ChatScreen. The hook handles everything internally.
  </action>
  <verify>TypeScript compilation: `cd /Users/turbolaunch/Apps/local-friend && npx tsc --noEmit src/screens/ChatScreen.tsx`</verify>
  <done>ChatScreen uses useConversationEnd hook for automatic memory extraction</done>
</task>

<task type="auto">
  <name>Task 2: Add memory store initialization to App</name>
  <files>src/App.tsx</files>
  <action>
Add memory store initialization on app startup:

1. Import memory store:
   ```typescript
   import { useMemoryStore } from './stores/memoryStore';
   ```

2. Add initialization alongside existing store loads (likely where chatStore.loadFromStorage is called):

   Find where app initialization happens (useEffect or similar) and add:
   ```typescript
   // Load persisted data
   useChatStore.getState().loadFromStorage();
   useMemoryStore.getState().loadFromStorage();
   ```

   If there's no initialization useEffect, create one:
   ```typescript
   useEffect(() => {
     // Load persisted state on app startup
     useChatStore.getState().loadFromStorage();
     useMemoryStore.getState().loadFromStorage();
   }, []);
   ```

The memory store must load before any chat interactions to ensure memories are available for prompt injection.
  </action>
  <verify>TypeScript compilation: `cd /Users/turbolaunch/Apps/local-friend && npx tsc --noEmit src/App.tsx`</verify>
  <done>App initializes memory store from MMKV on startup</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete memory system integration</what-built>
  <how-to-verify>
1. Build and run app: `cd /Users/turbolaunch/Apps/local-friend && npx expo run:ios`
2. Start a conversation mentioning personal details:
   - "My sister Sarah is visiting next week"
   - "I work as a nurse at the hospital"
   - "I've been feeling anxious about the move"
3. Background the app (press home or switch apps)
4. Check logs for "[MemoryOrchestrator] Stored X memories"
5. Return to app and start a NEW conversation (clear chat if needed)
6. Observe AI naturally referencing previous details (e.g., asks about Sarah's visit)
7. Verify app stays responsive during extraction (no freezing)

Expected behaviors:
- Extraction happens silently on background
- New conversations reference previous context
- No token budget errors in logs
- App remains stable throughout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with memory integration</resume-signal>
</task>

</tasks>

<verification>
- [ ] ChatScreen imports and uses useConversationEnd
- [ ] App initializes memory store on startup
- [ ] Memory extraction triggers on app background
- [ ] Memories surface in new conversations
- [ ] No crashes or freezes during extraction
- [ ] TypeScript compilation passes
</verification>

<success_criteria>
1. useConversationEnd hook active in ChatScreen
2. Memory store loads from MMKV on app startup
3. Memories extracted when user backgrounds app
4. AI references previous conversation details naturally
5. Token budget stays within limits (no errors)
6. App performance unaffected by extraction
</success_criteria>

<output>
After completion, create `.planning/phases/03-memory-system/03-06-SUMMARY.md`
</output>
