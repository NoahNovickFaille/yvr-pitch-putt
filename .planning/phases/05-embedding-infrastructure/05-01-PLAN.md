---
phase: 05-embedding-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/embedding.ts
  - src/types/embedding.ts
autonomous: true

must_haves:
  truths:
    - "Embedding model definition exists with correct URL and size"
    - "TypeScript types exist for embedding vectors and service state"
    - "Download URL resolves to valid GGUF file"
  artifacts:
    - path: "src/constants/embedding.ts"
      provides: "Embedding model definition with URL, size, config"
      contains: "EMBEDDING_MODEL"
    - path: "src/types/embedding.ts"
      provides: "EmbeddingVector, EmbeddingServiceState types"
      exports: ["EmbeddingVector", "EmbeddingServiceState"]
  key_links:
    - from: "src/constants/embedding.ts"
      to: "HuggingFace GGUF URL"
      via: "url field in model definition"
      pattern: "huggingface.co.*all-MiniLM"
---

<objective>
Add embedding model constants and TypeScript types for the embedding infrastructure.

Purpose: Establish the foundation for embedding model download and service implementation. This plan creates the model definition (URL, size, llama.rn config) and type definitions that all other embedding plans depend on.

Output: `src/constants/embedding.ts` and `src/types/embedding.ts` files ready for use by EmbeddingService and download system.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-embedding-infrastructure/05-RESEARCH.md

# Existing patterns to follow
@src/constants/model.ts
@src/types/memory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create embedding model constants</name>
  <files>src/constants/embedding.ts</files>
  <action>
Create `src/constants/embedding.ts` with:

1. `EmbeddingModelDefinition` interface (similar to ModelDefinition):
   - id: string
   - name: string
   - description: string
   - sizeBytes: number
   - url: string
   - filename: string
   - dimensions: number (output vector size)
   - maxTokens: number (context window)
   - llm config object (n_ctx, n_gpu_layers, use_mlock, embedding: true)

2. `EMBEDDING_MODEL` constant:
   - id: 'all-minilm-l6-v2'
   - name: 'All-MiniLM-L6-v2'
   - description: 'Semantic similarity model for memory deduplication'
   - sizeBytes: 22_000_000 (21MB with buffer)
   - url: 'https://huggingface.co/second-state/All-MiniLM-L6-v2-Embedding-GGUF/resolve/main/all-MiniLM-L6-v2-Q4_K_M.gguf'
   - filename: 'all-MiniLM-L6-v2-Q4_K_M.gguf'
   - dimensions: 256
   - maxTokens: 384
   - llm: { n_ctx: 384, n_gpu_layers: 99, use_mlock: true, embedding: true }

3. `EMBEDDING_STORAGE_KEYS` object:
   - DOWNLOAD_STATE: 'embedding_download_state'
   - MODEL_READY: 'embedding_model_ready'

4. `DEDUPLICATION_THRESHOLD` constant: 0.85 (similarity threshold for merging)

5. `getEmbeddingModelPath()` helper function:
   - Import documentDirectory from expo-file-system/legacy
   - Return `${documentDirectory}${EMBEDDING_MODEL.filename}`

Follow the pattern from `src/constants/model.ts` for structure and naming conventions.
  </action>
  <verify>
Run `npx tsc --noEmit` - no TypeScript errors related to embedding.ts
  </verify>
  <done>
EMBEDDING_MODEL constant exists with correct URL and all required config fields. getEmbeddingModelPath() function exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create embedding type definitions</name>
  <files>src/types/embedding.ts</files>
  <action>
Create `src/types/embedding.ts` with:

1. `EmbeddingVector` type:
   - Array of numbers (number[]) representing the 256-dimensional vector
   - Add JSDoc explaining this is output from all-MiniLM-L6-v2

2. `EmbeddingServiceStatus` union type:
   - 'idle' | 'initializing' | 'ready' | 'error' | 'unloaded'
   - Matches LLMStatus pattern from src/services/llm/LLMService.ts

3. `EmbeddingServiceState` interface:
   - status: EmbeddingServiceStatus
   - error?: string
   - Optional context reference (LlamaContext from llama.rn)

4. `SimilarityResult` interface:
   - similarity: number (0-1 cosine similarity)
   - memoryId: string
   - content: string (for debugging)

5. `DuplicateCheckResult` interface:
   - isDuplicate: boolean
   - existingMemory?: Memory (import from types/memory.ts)
   - similarity?: number

6. `MigrationProgress` interface:
   - total: number
   - completed: number
   - status: 'idle' | 'running' | 'complete' | 'error'
   - error?: string

Include JSDoc comments explaining each type's purpose. Export all types.
  </action>
  <verify>
Run `npx tsc --noEmit` - no TypeScript errors. All types are exported and can be imported.
  </verify>
  <done>
All embedding types exist with proper JSDoc documentation. Types can be imported from src/types/embedding.ts.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -r "EMBEDDING_MODEL" src/` shows the constant is defined
3. `grep -r "EmbeddingVector" src/` shows the type is defined and exported
4. EMBEDDING_MODEL.url is a valid HuggingFace URL to all-MiniLM-L6-v2-Q4_K_M.gguf
</verification>

<success_criteria>
- src/constants/embedding.ts exists with EMBEDDING_MODEL, EMBEDDING_STORAGE_KEYS, DEDUPLICATION_THRESHOLD, and getEmbeddingModelPath()
- src/types/embedding.ts exists with EmbeddingVector, EmbeddingServiceState, SimilarityResult, DuplicateCheckResult, MigrationProgress
- All files compile without TypeScript errors
- Model URL matches research: https://huggingface.co/second-state/All-MiniLM-L6-v2-Embedding-GGUF/resolve/main/all-MiniLM-L6-v2-Q4_K_M.gguf
</success_criteria>

<output>
After completion, create `.planning/phases/05-embedding-infrastructure/05-01-SUMMARY.md`
</output>
