---
phase: 06-semantic-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/retrieval.ts
  - src/types/retrieval.ts
autonomous: true

must_haves:
  truths:
    - "Retrieval weights are configurable constants"
    - "Semantic threshold separates retrieval from deduplication"
    - "Types define scoring structure for memories"
  artifacts:
    - path: "src/constants/retrieval.ts"
      provides: "Retrieval configuration constants"
      contains: "RETRIEVAL_WEIGHTS"
    - path: "src/types/retrieval.ts"
      provides: "Type definitions for semantic retrieval"
      exports: ["ScoredMemory", "RetrievalConfig", "RetrievalResult"]
  key_links:
    - from: "src/constants/retrieval.ts"
      to: "src/types/retrieval.ts"
      via: "RetrievalConfig type"
      pattern: "RetrievalConfig"
---

<objective>
Create retrieval constants and types for semantic memory retrieval.

Purpose: Establish the foundational types and configurable parameters that the SemanticRetrieval service will use. This separates configuration from logic and provides clear type contracts.

Output: Constants file with retrieval weights and thresholds, types file with ScoredMemory and RetrievalConfig interfaces.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-semantic-retrieval/06-RESEARCH.md
@src/constants/embedding.ts
@src/types/embedding.ts
@src/types/memory.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create retrieval constants</name>
  <files>src/constants/retrieval.ts</files>
  <action>
Create retrieval constants file with:

1. RETRIEVAL_WEIGHTS object (as const):
   - semantic: 0.5 (50% weight on semantic similarity)
   - decay: 0.3 (30% weight on recency/decay)
   - importance: 0.2 (20% weight on importance)

2. DEFAULT_RETRIEVAL_CONFIG object:
   - maxIdentity: 3 (max identity memories, always included)
   - maxTopicRelevant: 4 (max semantically similar memories)
   - maxRecent: 2 (max recent memories regardless of topic)
   - semanticThreshold: 0.4 (min similarity for topic relevance - NOT 0.85 which is for deduplication)

3. Export the type for RETRIEVAL_WEIGHTS using typeof.

Add JSDoc comments explaining:
- Why 0.4 threshold (retrieval threshold is lower than deduplication 0.85)
- The configurable nature of these constants
- Reference to SEM-02 requirement for weights
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit src/constants/retrieval.ts`</verify>
  <done>Constants file exists with RETRIEVAL_WEIGHTS and DEFAULT_RETRIEVAL_CONFIG exported</done>
</task>

<task type="auto">
  <name>Task 2: Create retrieval types</name>
  <files>src/types/retrieval.ts</files>
  <action>
Create retrieval types file with:

1. ScoredMemory interface:
   - memory: Memory (from ./memory)
   - semanticScore: number (0-1 from cosine similarity)
   - decayScore: number (0-1 from calculateDecay)
   - importanceScore: number (0-1 normalized from memory.importance)
   - finalScore: number (weighted combination)

2. RetrievalConfig interface:
   - maxIdentity: number (max identity memories)
   - maxTopicRelevant: number (max topic-relevant memories)
   - maxRecent: number (max recent memories)
   - semanticThreshold: number (min similarity for inclusion)

3. RetrievalResult interface:
   - identity: Memory[] (always included)
   - topicRelevant: Memory[] (semantically similar)
   - recent: Memory[] (recent regardless of topic)
   - scoredMemories: ScoredMemory[] (full scoring details for debugging)

Import Memory from ./memory. Add JSDoc comments for each interface explaining its purpose and usage.
  </action>
  <verify>TypeScript compiles: `npx tsc --noEmit src/types/retrieval.ts`</verify>
  <done>Types file exists with ScoredMemory, RetrievalConfig, and RetrievalResult exported</done>
</task>

</tasks>

<verification>
After completing all tasks:
1. Both files compile without errors
2. Constants are properly typed
3. Types reference Memory correctly from types/memory
</verification>

<success_criteria>
- src/constants/retrieval.ts exports RETRIEVAL_WEIGHTS and DEFAULT_RETRIEVAL_CONFIG
- src/types/retrieval.ts exports ScoredMemory, RetrievalConfig, RetrievalResult
- All types compile without TypeScript errors
- Weights sum to 1.0 (0.5 + 0.3 + 0.2)
</success_criteria>

<output>
After completion, create `.planning/phases/06-semantic-retrieval/06-01-SUMMARY.md`
</output>
